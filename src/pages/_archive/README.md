# _archive

Previous beta-era pages. **Not routed.** Preserved for reference (motion patterns, GSAP + Lenis scroll grid, image loading approach) — Vite tree-shakes anything not imported, so files here add zero bundle weight.

## Contents

| File | Notes |
|---|---|
| `HomePageV1.jsx` | Original `/` landing — beta CTA + GSAP photo-grid scroll animation. Replaced by the new product-focused homepage in `src/pages/HomePage.jsx`. |
| `ScrollTestPage.jsx` | Standalone experiment, previously routed at `/scroll-test`. |
| `ScrollTestPage.css` | Tile/grid CSS; imported by `HomePageV1.jsx` and `ScrollTestPage.jsx`. |

If you delete files here, also remove this README entry. If the folder becomes empty, remove the folder.
